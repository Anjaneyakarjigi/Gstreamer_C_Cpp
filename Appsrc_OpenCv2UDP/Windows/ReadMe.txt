/*************/
Program transfer webcam OpenCv VideoCapture Images to UDP server 
Run this program by below Command

/* Build project */
cmake -H. -Bbuild

/* Compile project */
cmake --build build  --config Release

execute.sh gives a sample for execution, just run execute.sh which runs program
